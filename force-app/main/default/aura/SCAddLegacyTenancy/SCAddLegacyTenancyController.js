({
    doInit : function(component, event, helper) {
        helper.doInit(component, event, helper);                  
    },
    selectedRowsMDMTenancy: function (component, event, helper) {
        helper.selectedRowsMDMTenancy(component, event, helper);
    },
    getSelectedTenancy: function (component, event, helper) {
        helper.getSelectedTenancy(component, event, helper);
    },
    handleAPICallCS: function (component, event, helper) {
        helper.handleAPICallCS(component, event, helper);
    },
    handleClose: function (component, event, helper) {
        helper.handleClose(component, event, helper);
    },
})